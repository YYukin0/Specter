{
  "task": "P6.5 Part 5 -- batch-invariance classification (non-algorithmic baseline)",
  "model": "deterministic position-one-hot FakeModel (hermetic)",
  "gammas": [
    1,
    3,
    5
  ],
  "seeds": [
    0,
    1,
    2,
    3,
    4
  ],
  "temperatures": [
    0.0,
    1.0
  ],
  "algorithm_batch_invariance": {
    "n_sequence_pairs": 180,
    "n_mismatch": 0,
    "max_abs_token_delta": 0,
    "first_divergence_tokens": [],
    "verdict": "batch-invariant: batched run_round == N single-sequence runs, bit-exact"
  },
  "specdiff_clean_path": {
    "all_NO_DIVERGENCE": true,
    "reports": [
      {
        "prompt": "hello world",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      },
      {
        "prompt": "speculative decode",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      },
      {
        "prompt": "a longer prompt here",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      },
      {
        "prompt": "kv",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      },
      {
        "prompt": "the quick brown fox",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      },
      {
        "prompt": "one two three four",
        "mechanism": "NO_DIVERGENCE",
        "offending_round": null
      }
    ]
  },
  "real_backend_increment_O2": {
    "status": "not run here -- needs real Qwen2.5 on MPS / a real int4 build",
    "paper": "arXiv:2607.17283 (Lossless but Not Free)",
    "expected_per_logit_delta_magnitude": 0.0058,
    "classification_contract": "when every structural signal (draft tokens, cache-position vectors, cache lengths, accept/reject decisions, n_accepted, post-rollback cache length) agrees between the serial and batched verify but the emitted tokens differ, specdiff.classify returns BACKEND_NONDETERMINISM -- i.e. it is reported as a backend batch-variance effect, not an algorithm bug, with the measured logit delta attached."
  },
  "headline": "On a noise-free model the batched decoder is bit-exactly the single-sequence decoder (max token delta 0 over 180 sequence pairs). Any divergence on a real quantized/MPS backend is therefore attributable to batch-variant kernels, and specdiff classifies it as non-algorithmic (BACKEND_NONDETERMINISM).",
  "elapsed_s": 1.453371583018452
}